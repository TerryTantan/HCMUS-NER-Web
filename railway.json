{
  "build": {
    "nixpacksConfigPath": "nixpacks.toml",
    "nixpacksPlan": {
      "providers": ["python"]
    }
  },
  "deploy": {
    "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload"
  }
}
