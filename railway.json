{
  "build": {
    "nixpacksConfigPath": "nixpacks.toml",
    "nixpacksPlan": {
      "providers": ["python"]
    }
  },
  "deploy": {
    "startCommand": "PYTHONPATH=./app_packages PATH=./app_packages/bin:$PATH python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload"  
}
}
